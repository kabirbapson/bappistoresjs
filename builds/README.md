# Builds output

All offline shop packages from `npm run build:share` are written here.

| Path | Description |
|------|-------------|
| `BappiStores-Share/` | Latest package — zip this folder for shop PCs |

If the folder is locked (app still running), the script creates `BappiStores-Share-<timestamp>/` instead.

This directory is gitignored except this README.
